{
  "private": true,
  "scripts": {
    "build": "parcel build src/manifest.json --dist-dir dist --no-cache --no-source-maps",
    "watch": "parcel watch src/manifest.json --dist-dir dist --no-cache --no-hmr"
  },
  "dependencies": {
    "console-log-level": "^1.4.1",
    "webextension-polyfill": "^0.10.0"
  },
  "devDependencies": {
    "@parcel/config-webextension": "^2.8.3",
    "parcel": "^2.8.3",
    "process": "^0.11.10",
    "util": "^0.12.5"
  }
}
